import org.junit.*;

public class PrintTest {
    @Test
    public void testOutputCurrentConsumption(){
        Assert.assertTrue(true);
        // In the outputCurrentConsumption method(), the same code to generate the test
        // output strings would be the same code that generates the expected output
        // strings. It would therefore be redundant to test the same code.
        // The key thing here would then be to test is whether the User class successfully
        // updates its data or not, in order for that data to be outputted on the GUI.
    }
}
